<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginPage">

        <!-- LOGIN SCREEN (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- REGISTER -->
        <activity android:name=".RegisterActivity" android:exported="false" />

        <!-- OTP VERIFICATION -->
        <activity android:name=".VerificationActivity" android:exported="false" />

        <!-- VISITOR HOME -->
        <activity android:name=".VisitorHomeActivity" android:exported="false" />

        <!-- STAFF -->
        <activity android:name=".StaffDashboardActivity" android:exported="false" />
        <activity android:name=".StaffLoginActivity" android:exported="false" />

        <!-- MISSING ACTIVITIES (ADD THESE!) -->
        <activity android:name=".PatientHubActivity" android:exported="false" />
        <activity android:name=".ArrivalCheckInActivity" android:exported="false" />
        <activity android:name=".SymptomCollectorActivity" android:exported="false" />

        <!-- STAFF HUB ACTIVITY -->
        <activity
            android:name=".StaffHubActivity"
            android:exported="true"
            android:label="Staff Hub">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>
